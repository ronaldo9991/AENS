kind = "web"
previewPath = "/"
title = "AENS – AI Enterprise"
version = "1.0.0"
id = "artifacts/aens"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18575

[services.development]
run = "pnpm --filter @workspace/aens run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/aens", "run", "build" ]
publicDir = "artifacts/aens/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18575"
BASE_PATH = "/"
